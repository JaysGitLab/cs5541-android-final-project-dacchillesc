<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.DeviceDefault.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>
    <style name="DialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:headerBackground">@color/black</item>
        <!--<item name="android:headerYearTextAppearance">@color/colorAccent</item>
        <item name="android:headerMonthTextAppearance">@color/colorAccent</item>
        <item name="android:headerDayOfMonthTextAppearance">@color/colorAccent</item>-->
        <item name="android:calendarTextColor">@color/black</item>
    </style>
</resources>
